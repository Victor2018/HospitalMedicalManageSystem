package com.goudan.ui;

import java.awt.BorderLayout;
import java.awt.Color;
import java.awt.Dimension;
import java.awt.Graphics;
import java.awt.Graphics2D;
import java.awt.Image;
import java.awt.Toolkit;
import java.awt.geom.AffineTransform;

import javax.swing.ImageIcon;
import javax.swing.JFrame;
import javax.swing.JLabel;
import javax.swing.JPanel;

public class PaintTest extends JFrame {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		PaintTest pt = new PaintTest();
		
		pt.setVisible(true);
		pt.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
	}
	public PaintTest() {
		super();
		setBounds(100, 100, 500, 500);
		final Ball ball = new Ball(this.getSize());
		final MyPanel panel = new MyPanel(ball);
		
		
		Image image = Toolkit.getDefaultToolkit().createImage("images\\image13.png");
		getContentPane().add(panel, BorderLayout.CENTER);
		panel.repaint();
		
		/*new Thread(){
			public void run(){
				for (int i =0; i < 1000; i++) {
					ball.move();
					panel.repaint();
					try {
						Thread.sleep(100);
					} catch (InterruptedException e) {
						// TODO Auto-generated catch block
						e.printStackTrace();
					}
				}
			}
		}.start();*/
	}
	
	
	class MyPanel extends JPanel{
		
		private Ball ball;
		
		public MyPanel(Ball ball){
			this.ball = ball;
		}
		@Override
		public void paintComponent(Graphics g) {
			
			
			Graphics2D g2 = (Graphics2D)g;
			g2.clearRect(0, 0, 500, 500);
			//g2.setColor(new Color(255,0,0));
			/*g2.drawImage(Toolkit.getDefaultToolkit().createImage("images\\f016.gif"), new AffineTransform() , 
					this);*/
			/*g2.drawImage(Toolkit.getDefaultToolkit().createImage("e:\\a.jpg"),
                    100,
                    100,
                    this);*/
			g2.setColor(Color.red);
			g2.drawLine(200,200, 300, 300);
			//g2.drawString("dafdsafdsa", 100, 100);
			
		}
	}
	
	class Ball {
		private int x;
		private int y;
		private int dx = 5;
		private int dy = 8;
		private int dwidth =20;
		private Dimension dim;
		public Ball(Dimension d){
			dim = d;
		}
		
		public int getX(){
			return x;
		}
		public int getY(){
			return y;
		}
		public int getRa(){
			return dwidth;
		}
		
		public void move(){
			x += dx;
			y += dy;
			
			if (x + dwidth > dim.getWidth() - 100) {
				dx = -dx;
			}
			if (x < 0) {
				dx = -dx;
			}
			if (y + dwidth > dim.getHeight() - 100) {
				dy = -dy;
			}
			if (y < 0) {
				dy = -dy;
			}
		}
		
		
	}
}


